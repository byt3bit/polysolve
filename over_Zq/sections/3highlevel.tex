
\paragraph{Generating the polynomial system}
Constructing a polynomial system corresponding to an LWE problem follows the method of Arora and Ge \cite{ICALP:AroGe11}, i.e. by using the polynomial
$f(x) = x \cdot \prod_{i=1}^{t} (x+i)(x-i)$, as described in \Cref{subsec:previous-algorithms}.
% In \Cref{subsec:previous-algorithms}, we presented the polynomial representation of the \slwe\ problem introduced by Arora and Ge~\cite{ICALP:AroGe11}. This representation is the starting point of our algorithm. More specifically, we highlight the importance of the polynomial $f(x) = x \cdot \prod_{i=1}^{t} (x+i)(x-i)$, which vanishes on all possible values of the error term, to construct a system of polynomial equations capturing the \slwe\ problem. 
% \luc{generalize to generic irreducible polynomial but give example with cyclotomic}
\input{sections/lwe_types_reduction.tex}



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% OLD IMAGE -- Switch to this.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% \begin{figure}[b!]
% \centering
% \resizebox{0.55\linewidth}{!}{
%   \input{figures/bigmatrix}
% }
% \caption{Block-wise representation of the algorithm. Each row block corresponds to polynomials of total degree $i$, from $d$ down to $1$. The left part of each rectangular matrix shows the diagonalized leading submatrix of degree-$i$ monomials, while the right part contains lower-degree terms. Each iteration produces a new rectangular matrix in this form.}\label{fig:bigmatrix}
% \end{figure}

\subsection{High-level Intuition of the Algorithm}
In this section, we provide the high-level description of our algorithm applied to the polynomial system $\mathcal{F}_{\mathrm{LWE}}$ derived from \slwe. Next, we will show how to adapt the parameters of the algorithm to solve the other two variants, namely \rlwe\ and \mlwe. 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% NEW IMAGE
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{figure}[b!]
\centering
\begin{tabular}{cc}
  \resizebox{0.60\textwidth}{!}{\input{figures/bigmatrix}} &
  \resizebox{0.33\textwidth}{!}{\input{figures/summary.tex}}
\end{tabular}
\caption{\textbf{Left:} Block-wise representation of the algorithm. Each row block corresponds to polynomials of total degree $i$, from $d$ down to $1$. The left part of each rectangular matrix shows the diagonalized leading submatrix of degree-$i$ monomials, while the right part contains lower-degree terms. \textbf{Right:} Step-wise illustration of one iteration of the algorithm showing \texttt{Spoly}, \algdiag.}\label{fig:bigmatrix}
\end{figure}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

In the polynomial system $\mathcal{F}_{\mathrm{LWE}}$ (stemming from \slwe), derived from $m = \binom{n+d-1}{d}$ samples, we identify each polynomial with its coefficient vector. The terms of each polynomial are ordered according to a fixed \emph{graded} monomial ordering by total degree. 
% in the vector space of multivariate polynomials in $n$ variables of (total) degree at most $d$, which has dimension $w = \binom{n+d}{d}$. Fixing a graded monomial ordering induces a canonicalordering by total degree.
This yields a matrix $M \in \Z_q^{m \times w}$ whose rows correspond to the input polynomials and $w = \binom{n+d}{d}$.
With high probability, these rows are linearly independent (see \Cref{pivots}) and the leading principal $m \times m$ submatrix of $M$ is invertible; diagonalizing it (computing its Smith Normal Form) produces a set of polynomials with leading terms of degree $d$ (outlined by the first square structure in \Cref{fig:bigmatrix}). Note that, because we are working over $\Z_q$, the resulting polynomials are not necessarily monic, but their leading terms are all distinct and cover all monomials of degree $d$.
The remaining columns form a dense block containing lower-degree monomials. Each polynomial in this representation has a unique term of degree $d$ corresponding to the leading term, and additional terms of lower degree. The leading terms are all distinct and, due to the linear independence of the rows, they cover all monomials of degree $d$.

% This structure can be represented as a rectangular coefficient matrix with a block decomposition: the left part, corresponding to a square block, is diagonalized and highlighted in Figure~\ref{fig:bigmatrix}, while the remaining columns form a dense block containing lower-degree monomials. 
% Each polynomial in this representation has a unique term of degree $d$ corresponding to the leading term, and additional terms of lower degree. 
% The leading terms are all distinct and, due to the linear independence of the rows, they cover all monomials of degree $d$.

From this system, we generate a selected set of $S$-polynomials each having total degree $ \leq d$. Next we reduce this set of $S$-polynomials by the current diagonalized system. Note that this process \emph{does not require division}, as it consists of linear combinations of the diagonalized polynomials. Each reduced $S$-polynomial has total degree exactly $d-1$.

The coefficients of these reduced $S$-polynomials are then assembled into a new coefficient matrix.
Provided that a sufficient number of such polynomials is generated, the leading principal submatrix of this matrix has full rank with overwhelming probability and can be diagonalized in the same manner as before. % TODO: specify the rank of the matrix
This yields a new diagonalized system whose leading terms cover all monomials of degree $d-1$. Then we compute a set of $S$-polynomials and their reduction as described before. 

This procedure is applied recursively, decreasing the degree at each iteration. Finally, the process terminates at degree $1$, where the resulting system consists of linear polynomials of the form $(x_j - s_j)$ for $j=1,\dots,n$, and directly reveal the secret vector $\vars{s}$. % TODO: check that the final system is indeed of this form.
\mlf{$\to a_j x_j - s_j.$}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


% \paragraph*{NEW Intuition of the Algorithm.} 
% Figure~\ref{fig:bigmatrix} provides a conceptual visualization of the algorithm by embedding the iterative steps into a single large matrix. Each row block corresponds to polynomials of a fixed degree $i$ ($1 \le i \le d$), with the leading principal submatrix highlighted to indicate the diagonalization of the corresponding polynomials. The remaining portion of each block represents the dense part of the polynomials formed by lower-degree monomials. 

% Iterations proceed from the top block (degree $d$) to the bottom block (degree $1$). At each step, $S$-polynomials of degree $i$ are computed from the previously diagonalized polynomials, and the resulting submatrix is diagonalized independently. This block-wise representation is purely conceptual: the algorithm never operates on the full matrix at once, but rather processes one row-block at a time.

% The final block, corresponding to degree $1$, produces linear polynomials of the form $\{ x_j - s_j \} _{j=1,\dots,n}$, which uniquely determine the secret vector $\vars{s}$ and signal the termination of the algorithm. This structured visualization emphasizes the iterative reduction of polynomial degree and the progressive diagonalization that underlies the algorithm's efficiency.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\paragraph*{Iterations (formal)}
The main algorithm (Algorithm~\ref{alg:alg}) has $d-1$ iterations, processing polynomials of total degree $i+1$ at iteration-$i$, starting with $i = d-1$ and decreasing by one at each iteration until $i=1$. 
At iteration-$i$, it performs the following operations:  

% \begin{definition}[$\deg_i$ iteration]
% For $1 \le i \le d-1$, the \emph{$\deg_i$ iteration} refers to the stage in which polynomials with leading terms of degree $i$ are processed. Specifically, $S$-polynomials of degree $i$ are generated and subsequently diagonalized.
% \end{definition}

% With this notation, the $\deg_i$ iteration corresponds to the $(d-i)$-th step of the algorithm. Each $\deg_i$ iteration consists of two main operations:

\begin{enumerate}
    \item \textbf{$S$-polynomial generation.} (\texttt{Spoly})
    We generate $\binom{n+i-1}{i}$ $S$-polynomials, ensuring that the set contains exactly as many equations as there are monomials of degree $i$.

    An $S$-polynomial between polynomials $f$ and $g$ is computed if the greatest common divisor of their (distinct) leading terms has degree $i$.
    We recall that at this stage, $\lt(f)$ and $\lt(g)$ have degree $i+1$.
    The resulting $S$-polynomial has the form  
    \begin{equation}\label{eq:spoly}
        S_{fg} = \frac{\texttt{lcm}(\lc(f), \lc(g))}{\lc(f)} x_k\cdot f - \frac{\texttt{lcm}(\lc(f), \lc(g))}{\lc(g)} x_\ell \cdot g
    \end{equation}
    for suitable indices $k,\ell \in \{1,\dots,n\}$.
    As a consequence, $S_{fg}$ consists only of monomials of degree at most $i+1$.
    After reduction with respect to the starting set of polynomials, all terms in $S_{fg}$ have degree at most $i$.
    % TODO: explicitly say how the reduction is performed, and that it does not require division. Basically it is just an S-polynomial
    \item \textbf{Diagonalization.} (\algdiag)
    The newly generated matrix is diagonalized by computing the corresponding Smith Normal Form.  
    % \begin{enumerate}
    %     \item (\texttt{DiagA}) compute a Smith Normal Form to obtain an upper triangular matrix by eliminating entries below the diagonal;
    %     \item (\texttt{DiagB}) complete the diagonalization by eliminating entries above the diagonal. 
    % \end{enumerate}
  \end{enumerate}

  \begin{algorithm}[t!]
\caption{Main Algorithm}\label{alg:alg}
\begin{algorithmic}[1]
\Statex{\hspace*{-\algorithmicindent} \textbf{Input:} $\mathcal{P}_d$: initial set of samples, $n \geq 1$: secret's dimension, $d$: maximum error size}
\Statex{\hspace*{-\algorithmicindent} \textbf{Output:} $\mathcal{D}_1$: set of $n$ linear polynomials $\{ x_j - s_j: 1 \le j \le n \}$.}\mlf{$\to a_j x_j - s_j.$}
% \Statex{}
\State{\texttt{SET = Diag($\mathcal{P}_d, n, d$)}} \Comment{Initial conversion to Smith Normal Form of the degree-$d$ samples}
\For{$i = d-1, d-2, \dots, 1$}
    \State{\texttt{STEP1 = Spoly(SET,$n, i$)}} \Comment{$S$-polynomial generation step: $\mathcal{P}_i$}
    \State{\texttt{SET = Diag(STEP1,$n, i$)}} \Comment{Diagonalization step: Smith Normal Form computation $\mathcal{D}_i$}
\EndFor{}
\State{\Return{$\mathcal{D}_1$}} \Comment{Return the reduced set of linear polynomials}
\end{algorithmic}
\end{algorithm}

% The next section presents the algorithm in pseudocode and proves its correctness.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

In order to apply the same algorithm to the \rlwe\ and \mlwe\ cases, we need to adjust the parameters of the algorithm, in particular the number of samples $m$. More specifically
% \[ \begin{split}
% &\text{\rlwe} \rightarrow  \#variables = N \implies m_{\rlwe} = \binom{N+d-1}{d}, \\
% &\text{\mlwe} \rightarrow  \#variables = Nk \implies m_{\mlwe} = \binom{Nk+d-1}{d}.
% \end{split}\]
\begin{align*}
  \text{\rlwe} \rightarrow  \#variables = N &\implies m_{\rlwe} = \binom{N+d-1}{d}, \\
  \text{\mlwe} \rightarrow  \#variables = Nk &\implies m_{\mlwe} = \binom{Nk+d-1}{d}.
\end{align*}

\mlf{For a better understanding: Specify again what is $N$? }